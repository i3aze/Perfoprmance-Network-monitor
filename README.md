# Perfoprmance-Network-monitor
The app is pinging google every second and its pulling telemetry from PerfMon and showing on the taskbar.
Copy the code. Add the app.exe in startup in task manager and each time the PC starts the app will start the measurement of the telemetry.
The info is logged in a .csv file in {user}/Documents
